<Window x:Class="RevitAddin.WpfUi.Sample.Views.SimpleView"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RevitAddin.WpfUi.Sample.Views"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        mc:Ignorable="d">

    <StackPanel Margin="15">
        <TextBlock Text="Setting" Margin="4"/>
        <Button Content="Change Theme"
                Command="{Binding CommandChangeTheme}" />
    </StackPanel>
</Window>
